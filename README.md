# webshield-scanner 🛡

Automated Web Application Vulnerability Scanner

## Features
- Automated web crawling (configurable depth)
- XSS detection (reflected)
- SQL injection detection
- Security header audit
- HTML report generation

## Usage
```bash
pip install -r requirements.txt
python main.py http://target.com --depth 3 --output report.html
```
